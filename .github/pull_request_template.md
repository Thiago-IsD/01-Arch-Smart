## O que este PR faz

<!-- Uma ou duas frases. -->

## Checklist

- [ ] Query nova filtrada por `account_id` **e** com teste em `tests/isolation/`
- [ ] Nenhuma cor, URL, ID ou limite literal introduzido (token semântico / `.env` / config)
- [ ] Se é tela: os 5 estados (padrão, hover/foco, carregando, erro, vazio)
- [ ] Se é tela: testado em 390px e em 1440px
- [ ] Doc de módulo (`docs/dev/modulos/`) atualizada, se o comportamento mudou
- [ ] Migração Alembic incluída, se houve mudança de schema

Item que não se aplica a este PR: risque ou marque como feito com uma nota
curta do porquê, em vez de deixar sem marcar.
